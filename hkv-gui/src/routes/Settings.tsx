import { PlaceholderRoute } from "./PlaceholderRoute";

export function Settings() {
  return (
    <PlaceholderRoute
      eyebrow="Settings / Shell lane"
      title="Settings"
      description="Desktop preferences, local paths, and shell defaults arrive later."
      dependency="Depends on frontend state persistence and backend integration choices that will be defined later."
    />
  );
}
